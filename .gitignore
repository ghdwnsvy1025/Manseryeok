# Next.js
.next/
out/
node_modules/

# 환경 변수 (API 키 포함 - 절대 git에 올리면 안 됨)
.env.local
.env*.local

# 학습 데이터 (용량 크므로 제외)
data/knowledge.txt
data/embeddings.json

# OS
.DS_Store
Thumbs.db

# Phase 4 e2e bridge temp
scripts/.tmp/

# Playwright / Phase 6.1
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
e2e/.auth/
e2e/results.json
# E2E flag file written for next dev (also matched by .env*.local)
