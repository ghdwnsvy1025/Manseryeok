"use client";

/**
 * "나" 탭 — 하단 탭의 왼쪽 자리.
 *
 * 계획서(13절)의 3탭 재편 중 "나"에 해당한다.
 * 화면 내용은 MePage 가 그린다.
 */
import MePage from "@/components/me/MePage";

export default function Page() {
  return <MePage />;
}
